[package]
name = "archeaage-studio"
version = "0.1.0"
description = "ArcheaAge UI Studio — visual editor for the aa_ui client addon"
edition = "2021"

[lib]
name = "archeaage_studio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
